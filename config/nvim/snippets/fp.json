{
  "Gen Function $ (wrapped)": {
    "prefix": "zgen$",
    "body": ["Effect.gen(function* ($) {\n\t$0\n})"],
    "description": "Generator function with $ input"
  },
  "Gen Yield $": {
    "prefix": "yield$",
    "body": ["yield* $($0)"],
    "description": "Yield generator calling $()"
  },
  "Gen Yield $ (const)": {
    "prefix": "cyield$",
    "body": ["const $1 = yield* $($0)"],
    "description": "Yield generator calling $()"
  },
  "Import pipe": {
    "prefix": "ipipe",
    "body": ["import { pipe } from '@effect/data/Function';"]
  },
  "Import @effect/data module": {
    "prefix": "idata",
    "body": ["import * as $0 from '@effect/data/$0';"]
  },
  "Import @effect/io module": {
    "prefix": "ieffect",
    "body": ["import * as $0 from '@effect/io/$0';"]
  },
  "Import @effect/schema/Schema": {
    "prefix": "ischema",
    "body": ["import * as S from '@effect/schema/Schema';"]
  }
}
